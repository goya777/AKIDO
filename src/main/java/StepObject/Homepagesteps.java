/*
package StepObject;

import PageObject.HomepageElement;
import com.codeborne.selenide.Condition;

import java.time.Duration;

public class Homepagesteps extends HomepageElement {

    public Homepagesteps homevalue (String text){
        usernameinput.setValue(text);
        passwordinput.setValue(text).pressEnter();
        Loanbtn.click();
        coocky.click();
        calcbtn.click();
        personalId.setValue(text);
        documentid.setValue(text);
        checkbox.doubleClick();
        incomeamount.setValue(text).shouldBe(Condition.visible,Duration.ofMillis(5000)).pressTab();
        comment.setValue(text);
        contactpers.setValue(text);
        contractlastname.setValue(text);
        contactperphone.setValue(text);
        contactpersconect.setValue(text);
        income_statement_btn.click();
        loan_product_id.click();
        loan_product.setValue(text);

        return this;
    }

}
*/
